/** Profil pengguna (form / pengaturan). */

export interface IUserProfile {
  uid: string
  name: string
  email: string
  role: string
  avatar: string
  lastUpdated: string
  currency: string
  language: string
}
